Vui lòng sử dụng version Docker dưới đây để đảm bảo hệ thống thích nghi môi trường một cách tốt nhất

Docker version 29.2.1, build a5c7197
Docker Compose version v5.0.2



docker build -t your-app-name .


docker run -d -p 8761:8761 --network micro eureka-server

docker run -d -p 8888:8888 --network micro server-config


xóa 
docker stop <CONTAINER_ID>
docker rm <CONTAINER_ID>

dung hết tất cả service
docker stop $(docker ps -q)

xóa hết service 
docker rm $(docker ps -aq)

xóa sạch images
docker rmi $(docker images -q)


docker-compose up -d --build
docker compose up -d --build

// Build lại cụ thể service không build lại cả
docker compose up -d --build user-service


docker restart id

// Dọn dẹp ổ đĩa phân vùng của service nếu bị hết 
docker system prune -af
docker builder prune -af



pm2 restart my-app || pm2 start npm --name "my-app" -- start